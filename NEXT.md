# Recovery checkpoint

- **Published M3 authority:** PR #17 published head is `7e03eb9d88e8d7c9c46fb003ce310239f9cc57db`; its merged `main` checkpoint is `82fa460de578684a12a14c68361158b6f28155d7`. Forms and M3 are published and merged, not local-only.
- **Completed milestone:** **M3 Content verticals complete**: lifecycle, taxonomy, managed media, menus, SEO, and Forms.
- **Forms boundaries:** first-party `Form`, `FormField`, and `FormSubmission`; normalized reserved-safe slugs; active state; ten allow-listed field types; closed bounded configuration; deterministic/atomic ordering; strict known-key server validation; CSRF, honeypot and throttling; PRG; immutable label/type/config snapshots; conservative deletion; paginated inbox/detail; escaped Pulse interfaces; `forms.manage` with verified active identity and privileged MFA; metadata-only append-only audits. Export, notification, attachments, autoresponders, redirects, and automation were not justified and are not implemented.
- **Preserved boundaries:** all M1 identity/MFA/RBAC/session/audit, M2 Pulse UI, and M3 lifecycle/taxonomy/media/menu/SEO/sitemap/robots behavior remains covered by the complete regression suite.
- **Verification:** focused Forms suite: 6 tests/47 assertions. Complete suite: 73 tests/503 assertions. Baseline validation, routes, build, fresh SQLite migration, and Forms rollback/reapply were run; final command outcomes belong in the implementation PR and commit checkpoint.
- **Environment gates:** TD-005 browser/assistive-technology interaction remains outstanding because no compatible runner has been established. Real MySQL remains a release-environment gate; SQLite success is not MySQL proof. No SMTP, CSV export, or notification delivery is claimed.
- **Completed milestone:** **M4 Builder V4** implements the first-party schema-v1 document, stable UUID nodes, closed registry, bounded nesting/settings, managed media references, safe links/video, atomic optimistic saves, persisted snapshot templates, fail-closed legacy rendering, keyboard controls, and version-aware local recovery.
- **Builder compatibility:** legacy unversioned arrays remain stored and recoverable but do not render or silently migrate. Opening Builder reports the condition; retain/export historical data before intentionally replacing it. Raw HTML and arbitrary embeds are not V4 capabilities.
- **Environment gates:** browser/assistive-technology TD-005 and real MySQL remain outstanding. SQLite and CLI-server evidence are not substitutes for either gate.
- **CLI-server acceptance:** the file-session run exercised login, MFA, Forms administration, active GET, valid/invalid POST, inactive rejection, submissions, Builder edit/save and public rendering with empty server stderr and Laravel log. The array-session run exercised login/public/admin denial request boundaries with empty stderr/log; Laravel's intentionally non-persistent `array` driver cannot carry authentication or CSRF state across separate curl requests, so stateful array workflows remain covered in-process rather than misreported as a real multi-request session.
- **Next exact milestone:** **M5 Theme platform**. Do not begin M5 until M4 is published and merged.
