# Recovery checkpoint

- **Completed milestone:** M1 identity and deny-by-default RBAC remains complete. The M2 foundation is merged at `6e490d9`, the administration presentation bridge is merged at `4b7ee11` (focused commit `9370101`), taxonomy conversion is merged at `991daca` (focused commit `33fd311`), and the clean-runtime smoke repair is merged at `b769a1d` (focused commit `47a9209`). This checkpoint directly converts page and post resource lists and editors to reusable Pulse components while preserving their existing workflows and authorization boundaries.
- **Security/accessibility preserved:** Existing active-account, verification, privileged MFA, canonical permission, ownership/delegation, final-super-administrator, throttle, session, and append-only audit boundaries remain unchanged. Page and post screens now use labelled controls, native keyboard-operable checkboxes, responsive resource tables, escaped component output, shared toasts, and focus-restoring destructive confirmation instead of the legacy presentation bridge or native browser dialogs.
- **Current branch:** `work`.
- **Verification for this slice:** Dependencies were restored from lock files. `composer validate --strict`, `vendor/bin/pint --test`, the complete 36-test/214-assertion PHP suite, `php artisan route:list`, and `npm run build` pass. After `php artisan optimize:clear`, the documented isolated CLI-server login smoke passes with both file and array sessions: each response is HTTP 200 and contains `Sign in to Pulse`, while captured stderr and the fresh Laravel log are empty. The first narrow test attempt failed only because the generated Vite manifest was absent; after the required asset build, the 8-test/93-assertion `AdminShellTest` passes. No compatible Chromium, Chrome, Playwright, or browser runner is installed, so browser interaction and screenshot verification were not performed.
- **Next exact milestone:** Continue M2 direct component conversion with media, then menus, themes, plugins, settings, SEO, and builder Blade markup. Remove superseded bridge selectors only after their final consumers are gone. Run browser keyboard/focus/dialog/drawer/toast behavior and representative mobile/tablet/desktop screenshots if a compatible Chromium installation becomes practical. Then begin M3 content verticals in ROADMAP dependency order.
- **Unresolved issues:** Legacy module domain implementations still mix inline validation/workflow, contain unbounded queries, and lack complete adversarial coverage; presentation conversion does not represent them as hardened. Full remaining component conversion, browser/assistive-technology inspection, legacy `users.role`, predictable seed credentials, complete audit administration/retention, and M3–M11 remain.
- **Blockers:** No Git remote is configured, so push remains impossible; use the available PR workflow after the focused local commit. Browser interaction remains unavailable because no compatible browser runner is installed. There are no application-runtime blockers.
