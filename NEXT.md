# Recovery checkpoint

- **Completed milestone:** The first M3 vertical slice, production hardening of the page/post lifecycle, is implemented on `work`. M2 completion remains merged at `7931af9` (focused M2 commit `00b7781`).
- **Delivered boundaries:** Four typed lifecycle states, centralized Form Requests/action persistence, publication-time invariants, separate database-backed page/post slug namespaces with reserved page-route protection, shared public visibility scopes across frontend and sitemap, atomic bounded scheduled publication, signed authenticated private previews, optimistic editor locking, bounded/eager administration queries, transactional taxonomy/special-page mutations, content audits, and publication indexes.
- **Preserved boundaries:** M1 identity/MFA/RBAC/session/audit protections and all M2 Pulse components, shell, toasts/dialogs, deterministic Vite assets, native-dialog prohibition, and clean-runtime smoke procedure remain intact.
- **Next exact milestone:** Continue M3 in ROADMAP dependency order with taxonomy lifecycle hardening, then media security/processing. Convert featured-media URL fields only as part of the media referential-integrity slice. Builder schema and compatible full revision history remain M4 work.
- **Unresolved risks:** TD-005 browser/assistive-technology coverage is environment-blocked. TD-007 records inherited media URL, absent hierarchy, and revision boundaries. MySQL migration and overlap behavior require release-environment integration verification in addition to SQLite tests.
- **Operations:** Run `* * * * * php artisan schedule:run` from `cms/`; timestamps use configured application time at input and UTC storage in production. `content:publish-scheduled --batch=100` is conditional/idempotent and capped at 1000.
- **Verification:** `composer validate --strict`, `vendor/bin/pint --test`, focused 3-test/12-assertion content suite, complete 41-test/310-assertion PHP suite, `php artisan route:list`, production Vite build, environment-isolated `php artisan optimize:clear`, `git diff --check`, and real CLI-server login smoke with file and array sessions pass. Both smoke variants returned HTTP 200 with the expected heading and empty stderr. No compatible browser runner was found, so browser interaction/screenshots were not performed.
