@extends('admin.identity.layout',['title'=>'Authentication challenge']) @section('identity-content')<p>Enter a current six-digit authenticator code or one unused recovery code.</p><form method="POST" action="{{ route('admin.mfa.verify') }}" class="p-form">@csrf<x-pulse.field name="code" label="Authentication or recovery code" required autofocus autocomplete="one-time-code"/><x-pulse.button type="submit">Verify identity</x-pulse.button></form><form method="POST" action="{{ route('admin.logout') }}" class="p-actions">@csrf<x-pulse.button type="submit" variant="subtle">Sign out</x-pulse.button></form>@endsection
