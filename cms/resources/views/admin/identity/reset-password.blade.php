@extends('admin.identity.layout',['title'=>'Choose a new password']) @section('identity-content')<form method="POST" action="{{ route('admin.password.update') }}" class="p-form">@csrf<input type="hidden" name="token" value="{{ $token }}"><x-pulse.field name="email" label="Email address" type="email" :value="$email" required autocomplete="email"/><x-pulse.field name="password" label="New password" type="password" required autocomplete="new-password"/><x-pulse.field name="password_confirmation" label="Confirm password" type="password" required autocomplete="new-password"/><x-pulse.button type="submit">Reset password</x-pulse.button></form>@endsection
