@extends('admin.layouts.app',['heading'=>'Application logs'])
@section('content')<x-pulse.page-header title="Protected logs" description="A redacted, bounded tail of recognized application logs." />@include('admin.operations._nav')<x-pulse.card><form method="GET"><x-pulse.field label="Log file" name="file"><select name="file">@foreach($files as $file)<option @selected($log['file']===$file)>{{ $file }}</option>@endforeach</select></x-pulse.field><x-pulse.field label="Contains" name="search"><input name="search" maxlength="100" value="{{ request('search') }}"></x-pulse.field><x-pulse.button type="submit">Filter bounded tail</x-pulse.button></form></x-pulse.card><x-pulse.card><h2>{{ $log['file'] ?? 'No application log' }}</h2><pre class="p-log" tabindex="0">@foreach($log['lines'] as $line){{ $line }}
@endforeach</pre></x-pulse.card>@endsection
