@extends('admin.layouts.app',['title'=>'Roles','heading'=>'Roles']) @section('content')<x-pulse.page-header title="Roles" description="Define reusable permission sets without exceeding your authority."><x-slot:actions><a class="p-button" href="{{ route('admin.roles.create') }}">Add role</a></x-slot:actions></x-pulse.page-header><x-pulse.errors/><x-pulse.card>@if($roles->isEmpty())<x-pulse.empty title="No roles found">Create a custom role to group permissions.</x-pulse.empty>@else<x-pulse.table><thead><tr><th>Role</th><th>Type</th><th>Users</th><th>Actions</th></tr></thead><tbody>@foreach($roles as $role)<tr><td data-label="Role"><strong>{{ $role->label }}</strong></td><td data-label="Type">{{ $role->is_system?'Protected system role':'Custom role' }}</td><td data-label="Users">{{ $role->users_count }}</td><td data-label="Actions">@if(!$role->is_system)<div class="p-actions"><a class="p-button p-button--secondary" href="{{ route('admin.roles.edit',$role) }}">Edit</a><form method="POST" action="{{ route('admin.roles.destroy',$role) }}">@csrf @method('DELETE')<x-pulse.button variant="danger" data-confirm data-confirm-title="Delete role?" data-confirm-message="Assigned users will lose the permissions supplied by this role.">Delete</x-pulse.button></form></div>@else<span class="p-muted">Managed by Pulse</span>@endif</td></tr>@endforeach</tbody></x-pulse.table><x-pulse.pagination :paginator="$roles"/>@endif</x-pulse.card>@endsection
