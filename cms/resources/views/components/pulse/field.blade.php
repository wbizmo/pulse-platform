@props(['name', 'id' => null, 'label', 'type' => 'text', 'help' => null, 'required' => false, 'value' => null])
@php($controlId = $id ?? $name)
<div class="p-field"><label class="p-label" for="{{ $controlId }}">{{ $label }} @if($required)<span class="p-required" aria-hidden="true">*</span><span class="sr-only">(required)</span>@endif</label><input id="{{ $controlId }}" name="{{ $name }}" type="{{ $type }}" value="{{ $type === 'password' ? '' : old($name, $value) }}" @required($required) @error($name) aria-invalid="true" aria-describedby="{{ $controlId }}-error" @enderror {{ $attributes->class('p-input') }}>@if($help)<small class="p-help">{{ $help }}</small>@endif @error($name)<span id="{{ $controlId }}-error" class="p-error">{{ $message }}</span>@enderror</div>
