@props(['name', 'label', 'type' => 'text', 'help' => null, 'required' => false, 'value' => null])
<div class="p-field"><label class="p-label" for="{{ $name }}">{{ $label }} @if($required)<span class="p-required" aria-hidden="true">*</span><span class="sr-only">(required)</span>@endif</label><input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" value="{{ $type === 'password' ? '' : old($name, $value) }}" @required($required) @error($name) aria-invalid="true" aria-describedby="{{ $name }}-error" @enderror {{ $attributes->class('p-input') }}>@if($help)<small class="p-help">{{ $help }}</small>@endif @error($name)<span id="{{ $name }}-error" class="p-error">{{ $message }}</span>@enderror</div>
