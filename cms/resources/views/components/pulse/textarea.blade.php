@props(['name','label','value'=>null,'required'=>false])<div class="p-field"><label class="p-label" for="{{ $name }}">{{ $label }} @if($required)<span class="p-required">*</span>@endif</label><textarea class="p-textarea" id="{{ $name }}" name="{{ $name }}" @required($required) {{ $attributes }}>{{ old($name,$value) }}</textarea>@error($name)<span class="p-error">{{ $message }}</span>@enderror</div>
