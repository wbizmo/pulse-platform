@props(['name','id'=>null,'label','value'=>null,'required'=>false])@php($controlId = $id ?? $name)<div class="p-field"><label class="p-label" for="{{ $controlId }}">{{ $label }} @if($required)<span class="p-required" aria-hidden="true">*</span><span class="sr-only">(required)</span>@endif</label><textarea id="{{ $controlId }}" name="{{ $name }}" @required($required) @error($name) aria-invalid="true" aria-describedby="{{ $controlId }}-error" @enderror {{ $attributes->class('p-textarea') }}>{{ old($name,$value) }}</textarea>@error($name)<span id="{{ $controlId }}-error" class="p-error">{{ $message }}</span>@enderror</div>
