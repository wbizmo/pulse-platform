@extends($themeRuntime->view('layout'))
@section('content')<section class="pulse-section"><div class="pulse-container"><p class="pulse-eyebrow">Catalogue</p><h1>{{ isset($category) ? $category->name : 'Products' }}</h1>@isset($category)<p>{{ $category->description }}</p>@endisset<div class="pulse-card-grid">@forelse($products as $product)<article class="pulse-card">@if($product->featuredMedia)<img src="{{ $product->featuredMedia->public_url }}" alt="{{ $product->featuredMedia->alt_text ?: $product->name }}">@endif<h2><a href="{{ route('catalogue.show',$product->slug) }}">{{ $product->name }}</a></h2><p>{{ $product->short_description }}</p>@if($variant=$product->variants->first())<p><strong>{{ $variant->money->format() }}</strong> · {{ $variant->stock_status }}</p>@endif</article>@empty<p>No active products are available.</p>@endforelse</div>{{ $products->links() }}</div></section>@endsection
